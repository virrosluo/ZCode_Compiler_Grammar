func main() begin
            var x <- readInteger()
            var res <- 0
            for x until x < 0 by -1 begin
                res <- res + x
            end
            printInteger(res)
        end

        